/**
 * We build our own custom HTML elements with React.
 *
 * Component is basically just a custom HTML element, which we create using the Declarative approach ie with react we defined the desired target state and then react is responsible for generating and running the actual DOM instructions which updates whats visible on the screen
 *
 *
 *
 *
 *
 *
 */
