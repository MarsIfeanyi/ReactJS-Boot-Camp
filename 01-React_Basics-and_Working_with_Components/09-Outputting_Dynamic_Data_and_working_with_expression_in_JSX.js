/**
 * You can replace hardcoded data by using the curly bracket {} and you can also run JS expressions inside the curly braces.
 * React treats everything inside the curly bracket as JS expression.
 *
 * Hint: You declear the variables outside the component's return statement and then you will use curly braces {} and point the variables decleared outside the return statement ie just put in the name of the decleared variable inside the {}
 */
