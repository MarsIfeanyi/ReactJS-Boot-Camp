/**
 * Hint: In react component there is one important rule,regarding the HTML (JSX) code that is returned inside of a component, You MUST have only one root element per return statement or per JSX code snippet
 */
