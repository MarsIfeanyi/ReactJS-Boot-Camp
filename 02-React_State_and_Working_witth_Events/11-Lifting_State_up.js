/**
 * Hint: We move data from child to parent component by utilizing props to receive a function from the parent component which we call in the child component.
 *
 * We don't have direct connection between two sibling components, instead we can only communicate from parent to child and from child to parent
 *
 * Lifting the State up is moving data from the child component to some parent component (Bottom-up) to either use it there or to then pass it down to some other child component
 */
